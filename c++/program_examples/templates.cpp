#include<bits/stdc++.h>
using namespace std;

//parametrised classes
template<class t>
//tcan be any data type
class vector{
    t*arr;
    public:
    vector(t*arr){}
};

int main(){
    vector<int>myvec(ptr);
    vector<char>myvec1(ptr1);

    return (0);
}